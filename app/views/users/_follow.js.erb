
  var id = '<%= @user_id %>'
  var posts = document.querySelectorAll(`.user-post-${id}`);
  console.log(posts);
  posts.forEach((post) => {
    post.innerText= "";
  });


  var feedFollows = document.getElementById('feed-follows')
  var html = "<%=j render 'shared/follow_feed', posts: @my_new_followed_posts %>";

  feedFollows.innerHTML = `${html}`;


