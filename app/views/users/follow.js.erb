
var id = '<%= @user_id %>'
var posts = document.querySelectorAll(`.user-post-${id}`);
posts.forEach((post) => {
  post.innerText=""
});





// const feedFollows = document.getElementById('feed-follows');
// '<% @html_string = "" %>';
// '<% @my_new_followed_posts.each { |post| @html_string += j render "shared/follow_feed_card", post: post }%>';

// feedFollows.innerHTML='<%= @html_string %>'


